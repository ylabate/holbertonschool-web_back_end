#!/usr/bin/env python3
"""Module for converting floats to strings with type annotations."""


def to_str(n: float) -> str:
    """Convert a float to its string representation.

    Args:
        n: A float number to be converted to a string.

    Returns:
        A string representation of the float.
    """
    return str(n)
